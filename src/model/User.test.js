describe('User', () => {
    describe('auction behaviour', () => {
        xtest('a user can create an auction', () => {
        });

        describe('auction betting', () => {
            xtest('a user can not bet in his own auction', () => {
            });
            xtest('a user can not bet in an auction if it is new', () => {
            });
            xtest('a user can bet in an auction if it is in progress', () => {
            });
            xtest('a user can not bet in an auction if it is finished', () => {
            });
        });
    });
});
